services:
  nextjs-app:
    build:
      context: .
      dockerfile: Dockerfile
    image: my-nextjs-app:latest
    container_name: nextjs-web
    ports:
      - "3000:3000"
    restart: always
    env_file:
      - .env.production # ระบุไฟล์ env ที่จะใช้บน server